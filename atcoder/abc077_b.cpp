#include <iostream>
#include <math.h>

using namespace std;
using ll = long long;

int main(int argc, const char *argv[])
{
    ll n;
    cin >> n;
    cout << (int)sqrt(n) * (int)sqrt(n) << endl;
    return 0;
}
